p = 0;
Phi = 0;
R = zeros(3,3);
A = zeros(4,4);
T = zeros(4,4);
q = [-210 0 0 0 0 0 pi/2 0];
EulerAngles = '
%[p, Phi, R, A] = cindir(q, 'RPY');

Ja = zeros([6,6]);
Ja = jacobianoAnalitico(q, EulerAngles);

